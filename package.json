{
	"name": "clara",
	"private": true,
	"workspaces": [
		"packages/api",
		"packages/app",
		"packages/plugins/*"
	],
	"scripts": {
		"dev": "bun run --filter clara tauri dev",
		"build": "bun run --filter clara tauri build"
	}
}
